import { AsRepeatDirective } from './as-repeat.directive';

describe('AsRepeatDirective', () => {
  it('should create an instance', () => {
    const directive = new AsRepeatDirective();
    expect(directive).toBeTruthy();
  });
});
